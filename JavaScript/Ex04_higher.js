function higher(num1, num2) {
    let higher;
    if (num1 > num2) {
        return higher = num1;
    } else if (num2 > num1) {
        return higher = num2;
    } else {
        return num1 + " is equal to " + num2
    }
}